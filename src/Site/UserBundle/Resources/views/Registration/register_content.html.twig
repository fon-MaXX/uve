{% trans_default_domain 'FOSUserBundle' %}

<div class="form-wrap">
    {#{{ form_start(form, {'method': 'post', 'action': path('fos_user_registration_register'), 'attr': {'class': 'fos_user_registration_register ordering-options-form'}}) }}#}
    <form id="user-register-form" method ="post" action="{{ path('fos_user_registration_register') }}" class="fos_user_registration_register ordering-options-form" />
        <div class="login-form-wrapper" >
            <fieldset class="col-lg-12 col-sm-12 col-xs-12 no-padding">
                {{ form_label(form.username) }}
                {% if form.username.vars.valid==false %}
                    <div class="error">
                        {{ form_errors(form.username) }}
                    </div>
                {% endif %}
                <hr class="register-hr-required"/>
                {{ form_widget(form.username) }}
                <p>Введите предпочитаемый логин (можно использовать псевдоним на русском языке). Логин не может начинаться и заканчиваться пробелом.</p>
            </fieldset>
            <fieldset class="col-lg-12 col-sm-12 col-xs-12 no-padding">
                {{ form_label(form.email) }}
                {% if form.email.vars.valid==false %}
                    <div class="error">
                        {{ form_errors(form.email) }}
                    </div>
                {% endif %}
                <hr class="register-hr-required"/>
                {{ form_widget(form.email) }}
                <p>На указанный адрес будут отправляться письма с сайта. Почтовый адрес не будет доступен другим пользователям и используется для восстановления пароля, определённых вами оповещений и связи через контактную форму.</p>
            </fieldset>
            <div class="col-lg-12 col-sm-12 col-xs-12 no-padding">
                <div class="profile-edit-label col-lg-12 no-padding">
                    {{ form_label(form.avatar) }}
                </div>
                <div class="profile-edit-input col-lg-12 no-padding">
                    {{ form_widget(form.avatar) }}
                </div>
            </div>
            <div class="col-lg-12 col-sm-12 col-xs-12 no-padding">
                <div class="profile-edit-label col-lg-12 no-padding">
                    {{ form_label(form.phone) }}
                </div>
                <div class="profile-edit-input col-lg-12 no-padding">
                    <div class="col-lg-1 col-sm-1 col-xs-1 text-right no-padding profile-order-label-small no-padding">+38</div>
                    <div class="col-lg-11 col-sm-11 col-xs-11 profile-user-phone-input no-padding">{{ form_widget(form.phone) }}</div>
                </div>
                {% if form.phone.vars.valid==false %}
                    <div class="profile-edit-label profile-edit-error col-lg-12 no-padding">
                        {{ form_errors(form.phone) }}
                    </div>
                {% endif %}
            </div>
            <fieldset class="col-lg-12 col-sm-12 col-xs-12 no-padding">
                {{ form_label(form.plainPassword.first) }}
                {% if form.plainPassword.vars.valid==false %}
                    <div class="error">
                        Введенные пароли не соответствуют друг другу
                    </div>
                {% endif %}
                <hr class="register-hr-required"/>
                {{ form_widget(form.plainPassword.first) }}
            </fieldset>
            <fieldset class="col-lg-12 col-sm-12 col-xs-12 no-padding">
                {{ form_label(form.plainPassword.second) }}
                <hr class="register-hr-required"/>
                {{ form_widget(form.plainPassword.second) }}
            </fieldset>
            <div class="display-none">
                {{ form_rest(form) }}
            </div>
        </div>
    <div class="col-md-12 inline-block no-padding">
        <button type="submit" id="register-form-submit-button" class="top-button margin-20-0" >РЕГИСТРАЦИЯ</button>
    </div>
    </form>
</div>
